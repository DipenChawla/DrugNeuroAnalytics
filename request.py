import requests
from wtforms import Form,StringField,TextAreaField, PasswordField, validators

# url = 'http://localhost:5000/api'
#
# r = requests.post(url,json={'review':'halucinations were faced',})
# print(r.json())
